# Composer bot playbook

## §7 Change log for agents

- 2026-04-17 — **T-03** (net-worth trend): `NetWorthSeriesPoint` / `NetWorthSeriesWindow` in `src/finance/types.ts`, `getNetWorthSeries` in `src/finance/ledger.ts` (optional `asOf` for tests), `NetWorthLineChart` web/native in `src/components/charts/`, Dashboard card with 3M/6M/12M/ALL pills in `src/pages/DashboardPage.tsx`, tests in `src/finance/ledger.test.ts`. Branch: `cursor/networth-trend-7909`.
