# Composer bot playbook (Ledgerline)

Sections §1–§6: see repository maintainer / product docs. This file exists for agent coordination.

## §7 Change log for agents

- 2026-04-17 — T-02: Envelope budgeting with rollover (`getBudgetEnvelopes`, `BudgetsPage` flow/envelope toggle, `DashboardPage` over-budget uses envelope `available` when envelope mode on). Branch `cursor/envelope-budgets-7f53`.
