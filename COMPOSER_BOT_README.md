# Ledgerline — Composer bot coordination

Full playbook sections §1–§6 are maintained outside this file (product backlog). This file only records agent completions in §7.

## 7. Change log for agents

- 2026-04-17: T-02 envelope budgeting with rollover — branch `cursor/envelope-budgets-d643` (Budget `rollover`/`carry`, `getBudgetEnvelopes`, Flow vs Envelope on Budgets, Dashboard over-budget uses envelopes when envelope mode is on, `ledger.test.ts`).
